# Tool / IDE folders
.claude/
.idea/
.vscode/

# Python / virtualenv
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.cache/
*.egg-info/
build/
dist/

# Environment
.env
.env.local

# Exception: Selenium Grid .env files contain no secrets (only port/host config)
# These must be committed so Jenkins can start the grid nodes
!06-jenkins-ci-cd/09-runner-approach-1/.env
!06-jenkins-ci-cd/10-runner-approach-2/.env

# macOS
.DS_Store
__MACOSX/

# Test output / generated reports
**/output/
**/reports/assets/
**/reports/*.html
**/reports/*.xml
**/allure-results/
**/allure-report/
**/logs/*.log
**/screenshots/*.png

# External course/reference repos kept locally only
selenium-docker/

# Jenkins runtime data (plugins, builds, logs — generated, not source code)
**/volumes/