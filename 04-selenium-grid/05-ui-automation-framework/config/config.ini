# =============================================================================
# config.ini — Environment configuration for the UI framework
# =============================================================================
# Switch environments:  export ENV=staging  (or set in Docker / CI)
# =============================================================================

[dev]
base_url         = https://the-internet.herokuapp.com
implicit_wait    = 0
explicit_wait    = 10
page_load_timeout = 30
browser          = chrome
headless         = false
screenshot_dir   = screenshots
driver_mode      = selenium_manager
# driver_mode options:
#   selenium_manager  → Selenium 4.6+ built-in, zero config needed
#   webdriver_manager → uses webdriver-manager pip package
#
# Selenium Grid options:
#   use_grid = false   → local browser (default for dev)
#   use_grid = true    → remote WebDriver via Selenium Grid hub
#   grid_url           → hub URL (override with env var GRID_URL)
use_grid         = false
grid_url         = http://localhost:4444/wd/hub

[staging]
base_url         = https://the-internet.herokuapp.com
implicit_wait    = 0
explicit_wait    = 15
page_load_timeout = 45
browser          = chrome
headless         = true
screenshot_dir   = screenshots
driver_mode      = selenium_manager
use_grid         = false
grid_url         = http://localhost:4444/wd/hub

[ci]
base_url         = https://the-internet.herokuapp.com
implicit_wait    = 0
explicit_wait    = 15
page_load_timeout = 60
browser          = chrome
headless         = true
screenshot_dir   = screenshots
driver_mode      = selenium_manager
# In CI/CD set USE_GRID=true + GRID_URL=http://selenium-hub:4444/wd/hub
# to route all tests through the Selenium Grid containers
use_grid         = false
grid_url         = http://localhost:4444/wd/hub

[common]
# Valid login credentials for the-internet.herokuapp.com /login
valid_username   = tomsmith
valid_password   = SuperSecretPassword!
invalid_username = baduser
invalid_password = wrongpass

# Allure / HTML report paths
allure_dir       = allure-results
html_report      = reports/report.html
